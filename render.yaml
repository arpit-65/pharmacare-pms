services:
  - type: web
    name: pharmacare-flask
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "python web/app.py"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
