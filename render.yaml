services:
  - type: web
    name: pharmacare-flask
    env: python
    buildCommand: "pip install -r requirements.txt"
    startCommand: "cd web && gunicorn app:app --bind 0.0.0.0:$PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.10.0
